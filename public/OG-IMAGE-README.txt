OG image for social sharing is now public/og-image.svg (1200×630).

It uses the same sparkle from the favicon, plus:
- "Beauty by Raquel Nicole"
- "Hair Stylist & Color Specialist"
- "Milford, OH"

Layout and schema reference /og-image.svg. If a platform does not support SVG (rare), you can export the SVG to PNG or JPG at 1200×630 and save as og-image.jpg, then point metadata back to /og-image.jpg.
