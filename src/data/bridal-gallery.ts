export type BridalGalleryItem = {
  id: string;
  title: string;
  imageUrl: string;
  alt: string;
};

/** Add photos to public/gallery/bridal/ and list them here when Raquel sends selections. */
export const BRIDAL_GALLERY_ITEMS: BridalGalleryItem[] = [];
